
이제 프론트엔드 측면을 살펴보겠습니다.
프론트엔드(클라이언트) 측면을 확인해 보겠습니다.

`nougat-playground-fe`에서 프론트엔드 부분을 확인해 보겠습니다.




이제 전체적인 그림이 잡혔습니다. 분석 결과를 정리해 드립니다.

---

## `/api/guest/init` 로직 분석

### 1. API 서버 (nougat-playground / api-server)

#### Controller — `@/srv/nougat-playground/apps/api-server/src/security/guest-auth.controller.ts:5-14`

```ts
@Controller('guest')
export class GuestAuthController {
  @Public()
  @Post('init')
  async initGuest() {
    return this.guestAuthService.initGuest();
  }
}
```

- 경로: `POST /api/guest/init` (NestJS 글로벌 prefix `/api` + `guest` + `init`)
- `@Public()` 데코레이터로 인해 [SecurityAuthGuard](cci:2://file:///srv/nougat-playground/apps/api-server/src/security/security-auth.guard.ts:20:0-241:1) 인증을 **우회**합니다.

#### Service — `@/srv/nougat-playground/apps/api-server/src/security/guest-auth.service.ts:10-24`

```ts
async initGuest() {
  const guestToken = randomUUID();
  const secret = randomBytes(32).toString('base64');
  const key = `guest:${guestToken}`;
  await redis.set(key, JSON.stringify({ secret }), 'EX', GUEST_TOKEN_TTL_SECONDS);
  return { guestToken, secret, expiresIn: GUEST_TOKEN_TTL_SECONDS };
}
```

- **guestToken**: `randomUUID()`로 생성 (클라이언트 식별용)
- **secret**: `randomBytes(32)` base64 인코딩 (HMAC 서명용 비밀키)
- Redis에 `guest:{token}` 키로 `{ secret }` 저장, **TTL 300초** (5분)
- 응답: `{ guestToken, secret, expiresIn: 300 }`

#### Rate Limiting — `@/srv/nougat-playground/apps/api-server/src/security/rate-limit.middleware.ts:25-28`

```ts
if (normalizedPath === '/api/guest/init') {
  await this.enforceLimit([ratelimit:${this.getClientIp(request)}](cci:1://file:///srv/nougat-playground/apps/api-server/src/security/security-auth.guard.ts:86:2-98:3), GUEST_INIT_RATE_LIMIT);
  return next();
}
```

- **IP 기준 분당 5회** (`GUEST_INIT_RATE_LIMIT = 5`) 제한
- 초과 시 `429 Too Many Requests`

#### 인증 가드 (이후 API 호출 시) — `@/srv/nougat-playground/apps/api-server/src/security/security-auth.guard.ts:30-73`

`/api/guest/init` 이외의 모든 `/api/` 요청은 [SecurityAuthGuard](cci:2://file:///srv/nougat-playground/apps/api-server/src/security/security-auth.guard.ts:20:0-241:1)를 거칩니다:

1. `Authorization: Guest {token}` 헤더에서 token 추출
2. Redis에서 `guest:{token}` 으로 secret 조회
3. `X-Timestamp` 헤더 검증 (±30초 허용)
4. `X-Nonce` + `X-Signature` 검증
   - **서명**: `HMAC-SHA256(secret, method + canonicalPath + normalizedBody + timestamp + nonce)`
   - **nonce**: Redis `SET NX`로 replay attack 방지 (30초 TTL)
5. `timingSafeEqual`로 서명 비교 (timing attack 방지)

---

### 2. 프론트엔드 (nougat-playground-fe)

#### Plugin — `@/srv/nougat-playground-fe/plugins/guest-init.client.ts:1-7`

```ts
export default defineNuxtPlugin(async () => {
  await ensureGuestSession().catch(() => {
    // First authenticated request will retry initialization.
  })
})
```

- 클라이언트 사이드 Nuxt 플러그인, 페이지 로드 시 자동 실행

#### Composable — `@/srv/nougat-playground-fe/composables/useApi.ts`

**세션 초기화 흐름** ([ensureGuestSession](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:338:0-347:1), [fetchGuestSession](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:311:0-336:1)):

1. **쿠키 복원** ([restoreGuestSessionFromCookieOnce](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:85:0-112:1)): `nougat_guest_session_v1` 쿠키에서 `{ token, secret, expiresAtMs }` 복원 (만료 5초 전까지만 유효)
2. **세션 체크**: 메모리에 token/secret이 있고 만료 30초 전이면 skip
3. **POST `/guest/init`** ([fetchGuestSession](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:311:0-336:1)): 빈 body로 POST 요청, 응답에서 `{ guestToken, secret, expiresIn }` 수신
4. **세션 적용** ([applyGuestSession](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:285:0-309:1)):
   - 메모리 변수에 token/secret 저장
   - `expiresAtMs = Date.now() + expiresIn * 1000` 계산
   - 쿠키에 세션 저장 (SameSite=Lax, Secure 조건부)
   - 만료 30초 전 자동 갱신 타이머 예약 ([scheduleRefresh](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:276:0-283:1))

**API 요청 시 인증 헤더 생성** ([getClientAuthHeaders](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:358:0-422:1)):

```ts
return {
  'Authorization': `Guest ${guestToken}`,
  'X-Timestamp': timestamp,      // Date.now()
  'X-Nonce': nonce,              // randomUUID or fallback
  'X-Signature': signature,      // HMAC-SHA256(secret, method+path+body+timestamp+nonce)
}
```

**401 재시도 로직** ([executeFetch](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:432:0-489:1)):

- 클라이언트에서 401 응답 시 → [refreshGuestSession()](cci:1://file:///srv/nougat-playground-fe/composables/useApi.ts:349:0-356:1) 호출 후 **1회 재시도** (`retried` 플래그로 무한 루프 방지)
- `/guest/init` 경로는 `skipAuth = true`로 인증 헤더 없이 요청

---

### 3. 전체 시퀀스 요약

```
[브라우저 로드]
  └─ guest-init.client.ts 플러그인
       └─ ensureGuestSession()
            ├─ 쿠키 복원 시도 → 유효하면 skip
            └─ POST /api/guest/init (인증 없음, IP당 5회/분 제한)
                 └─ 응답: { guestToken, secret, expiresIn: 300 }
                      └─ 메모리 + 쿠키 저장, 갱신 타이머 예약

[이후 API 호출]
  └─ apiFetch() / executeFetch()
       └─ getClientAuthHeaders(method, path, body)
            └─ HMAC-SHA256 서명 생성
                 └─ Authorization: Guest {token}
                    X-Timestamp, X-Nonce, X-Signature
       └─ 401 시 → 세션 갱신 후 1회 재시도
```

**핵심 특징**:
- **Stateless guest 인증**: JWT 대신 Redis 기반 단기 세션 + HMAC 서명
- **Replay attack 방지**: nonce + timestamp window (±30초)
- **자동 갱신**: 만료 30초 전 타이머 + 401 시 on-demand 갱신
- **쿠키 영속성**: 페이지 리로드 시 쿠키에서 세션 복원으로 중복 init 방지